---
all:
  children:
    llama:
      hosts:
        cloud-1:
    tts_engine:
      hosts:
        cloud-1:
    stt_engine:
      hosts:
        cloud-1:

  # ansible_host is the address Ansible dials; the inventory NAME stays stable
  # and human-meaningful even when the (ephemeral) box's address changes.
  hosts:
    cloud-1:
      ansible_host: REPLACE_ME_box_ip  # NEVER_COMMIT
