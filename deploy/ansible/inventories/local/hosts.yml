---
all:
  children:
    llama:
      hosts:
        local-1:
    tts_engine:
      hosts:
        local-1:
    stt_engine:
      hosts:
        local-1:

  hosts:
    local-1:
      ansible_host: localhost
      ansible_connection: local
